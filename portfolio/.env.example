# Same backend as nandex; /api/* is proxied here (see next.config.ts).
BACKEND_ORIGIN=http://localhost:8000
# Cal.com (or any scheduling) link. Unset: /book falls back to /message.
NEXT_PUBLIC_BOOKING_URL=
