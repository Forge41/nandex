# Django backend origin -- every /api/* request is proxied here (see next.config.ts),
# same-origin from the browser's perspective so the session cookie just works.
BACKEND_ORIGIN=http://localhost:8000
